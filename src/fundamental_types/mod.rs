// Make it pub so we can import it in other modules
// - `mod xxx` tells compiler that the module `xxx` lives in a separate file named `xxx.rs` in same dir
pub mod array_vec_slice;
pub mod bool_type;
pub mod char_type;
pub mod fixed_width_numeric;
pub mod string_types;
