// Vec supports efficiently adding and removing elements only at the end
// - `std::collenctions::VecDeque<T>` is a deque, a double-ended queue,
// - support efficiently adding and removing elements from both ends
